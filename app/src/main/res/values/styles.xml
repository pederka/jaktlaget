<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>   </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!-- Base application theme. -->

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="AppTheme.TextStyle" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">@dimen/text_medium</item>
    </style>

     <style name="AppTheme.TextStyle.Important" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">@dimen/text_huge</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.Button" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_button</item>
    </style>

    <style name="AppTheme.Button.Secondary" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimaryLight</item>
        <item name="android:textColor">@color/black</item>
    </style>

    // Card styles
    <style name="AppTheme.CardStyle">
        <item name="cardBackgroundColor">@color/colorPrimary</item>
    </style>
    <style name="AppTheme.CardText" parent="AppTheme.TextStyle">
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="AppTheme.CardText.Important" parent="AppTheme.TextStyle.Important">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
